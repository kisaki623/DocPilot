package com.docpilot.backend.ai.context;

public enum ContextType {
    SYSTEM,
    MODE_INSTRUCTION,
    MEMORY,
    SUMMARY,
    RECENT_TURN,
    RAG_EVIDENCE,
    CURRENT_MESSAGE,
    OUTPUT_REQUIREMENT
}
