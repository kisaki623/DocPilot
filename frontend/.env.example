# Backend base URL for Next.js proxy rewrite.
# Browser calls /backend/* on frontend origin, and Next.js forwards to this address.
BACKEND_BASE_URL=http://localhost:8081

# Optional: browser-direct backend address used by SSE stream API.
# If empty, local dev falls back to http://localhost:8081 for streaming.
NEXT_PUBLIC_BACKEND_BASE_URL=

